# Quoter - Java #

## Gradle Commands ## 
Build: `./gradlew build`
Clean: `./gradlew clean`
Run: `./gradlew bootRun`
Hot Reload (using two terminals): `./gradlew build --continous` && './gradlew bootRun`
